#include<stdio.h>
#include<string.h>
#define wsize 100010
#define nsize 65
#define msize 11
int dp[nsize][wsize];
int n,t,box,num;
int max(int a,int b)
{
    return a>b?a:b;
}
int main ()
{
    int i,j,k;
    while(scanf("%d %d",&n,&t)!=EOF)
    {
        memset(dp,-1,sizeof(dp));   //æä¾èµå³ç³»ï¼è¦èµåå¼-1
        memset(dp[0],0,sizeof(dp[0]));
        for(i=1;i<=n;i++)
        {
            scanf("%d %d",&box,&num);
            for(k=box;k<=t;k++)
                dp[i][k]=dp[i-1][k-box];  //åè®©iå±ä¹°çå­ï¼å ä¸ºçå­æ²¡æä»·å¼ï¼æä»¥ç´æ¥ç­äºä¸ä¸å±çè±è´¹+çå­é±
            for(j=0;j<num;j++)             //å¨å·²è±è´¹çå­é±çåºç¡ä¸ï¼æ­¤æ¶åå¯¹dp[i]å±å01èåï¼å³iå±ä¸ä¸ªçå­å¤ç§ç©åçæå¤§ä»·å¼       
            {
                int c,w;
                scanf("%d %d",&c,&w); 
                for(k=t;k>=c;k--)    
                {
                    if(dp[i][k-c]!=-1)  //æ³¨æä¾èµèåæä¸å¯è½çæåµï¼è¿éå³kä¹°ä¸å°çå­åè¿ä¸ªç©åï¼ä¸è½è£ç©å
                        dp[i][k]=max(dp[i][k],dp[i][k-c]+w);  // è¿éä¸è½dp[i][k]=max(dp[i][j],dp[i][k-box-c]+w) å ä¸ºå·²ç»                                        ä¹°è¿çå­äºï¼è¿ä¸ªè¡¨è¾¾å¼ä»£è¡¨ä¸ä¸ªçå­åºç¡ä¸ä¸ä¸ªç©åå¸¦ä¸ä¸ªçå­
                }
            }
            for(j=0;j<=t;j++)
            {               // ä¸ä¹°è¿ç»    ä¹°è¿ç»
                dp[i][j]=max(dp[i-1][j],dp[i][j]);  //ä¸è¦å¿äºèèä¸éå½åç»çæåµï¼ä¸æ¯å¿éï¼
            }
        }
        printf("%d\n",dp[n][t]);
    }
    return 0;
}